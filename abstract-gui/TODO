layouts
  AbsoluteLayout
  BorderLayout
  LinearLayout
  FlowLayout (?)
  GridLayout
advanced components, lists, menus etc., form builder
dialog util (with shortcut for alert, prompt, select etc.)
styles, metrics (px vs. cell vs. ?), basic css support (layouts?)
value restrition (integer, email, regex etc.)