metrics (px vs. cell vs. ?)
event handling
layouts (wrapper vs. immutable?)
  AbsoluteLayout
  BorderLayout
  LinearLayout
  FlowLayout (?)
  GridLayout
i18n
  ContentSource
    .{add|remove}ChangeListener(ChangeListener)
      .onChanged(...)
        or:
          - getSerial()
          - call from outer
          - ???
    .getContent()
advanced components (lists, menus etc.)
complex components (complete forms etc.)
basic css support (layouts?)
value restrition (integer, email, regex etc.)
dialog builder (with shortcut for alert, prompt, select etc.)